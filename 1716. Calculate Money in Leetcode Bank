class Solution {
    public int totalMoney(int n) {
        int weeks = n / 7, days = n % 7;
        int total = 0;
        total += (weeks * (7 * (7 + 1)) / 2) + (7 * (weeks * (weeks - 1)) / 2);
        total += days * (2 * (weeks + 1) + (days - 1)) / 2;
        return total;
    }
}