class Solution {
    public int smallestNumber(int n) {
        int bits = Integer.toBinaryString(n).length();
        int x = (1 << bits) - 1;
        if (x < n) x = (1 << (bits + 1)) - 1;
        return x;
    }
}
